# ルミナクエストIV 引き継ぎ（M0 時点）

最終更新：2026-08-05 ／ 状態：**M0 実装ずみ・未デプロイ**
構想書は `lq4/docs/LQ4_構想書.md`。物語・技の確定稿は同フォルダの3文書を正とする。本書は「いまどうなっているか」だけを書く。

---

## 1. 場所

| 項目 | 内容 |
|---|---|
| 公開URL（予定） | https://kouyamcz-svg.github.io/lumina-quest/lq4/ |
| リポジトリ | `kouyamcz-svg/lumina-quest`（`lq4/` 配下・lq3と同居） |
| 作業ディレクトリ | `/home/claude/lq4/` |
| **PAT** | **文書・メモリに保存しない。毎回依頼し、会話ごとに使い捨て・作業後に失効** |
| 取得方法 | API がレート制限に当たるときは `https://codeload.github.com/kouyamcz-svg/lumina-quest/tar.gz/refs/heads/main` で tarball 取得（トークン不要） |

## 2. M0 の到達点

垂直スライスが通しで動く（自動テストで確認ずみ、実機確認はこれから）。

イオの家 → 下層区 → 見習い試験場 → グランに話す → 木人戦 → セレン加入 → 裂け目の広場 → ウンブラ戦 → 章末 → セーブ/ロード復元

- lq4 は lq3 と干渉しない：`LQ4_SAVE_1〜3` ／ `CACHE='lq4-v1'` ／ `window.LQ4_BUILD` ／ `LQ4View`
- inflict（sleep/confuse/slow・単体/全体・ボスは p 半減・immune対応・**st2 で2段掛け**）と buff（atk/def/agi・nターン）が実装ずみ
- 検証ハーネス（構想書 §7 の 1〜5）が `test/` にそろっている

## 3. ファイル構成

```
lq4/
  index.html    ビルド成果物（src/*.js を結合）
  assets.js     ★lq3のものを仮置き（LQ4の絵は未着手）
  shell.html    HTML骨組み・PWAメタ（タイトル ルミクエⅣ）
  build.py      src/*.js → index.html
  sw.js         CACHE='lq4-v1'
  BUILD.txt     Pages再ビルド起こし用
  apple-touch-icon.png / icon-180.png / icon-512.png  ★lq3のものを仮置き
  src/          core / chapters / npc / world / view / view2d / ui / bgm
  docs/         構想書・詳細ストーリー・第3章五地方降下・技リスト・本書
  test/         下記
  *.mp3         ★lq3のものを暫定流用（field/town/castle/dungeon/battle3/boss）
```

## 4. テスト

```bash
cd /home/claude/lq4
sh test/all.sh                # ぜんぶ
node test/build_verify.js     # 40項目：構文・ビルド・残骸・せりふ46字
node test/audit.js            # 171項目：重複キー・参照実在・ワープ・BFS到達性
node test/battle_inflict.js   # 25項目：inflict / buff / st2
node test/gimmick.js          # 33項目：押し岩・穴・光珠灯・扉・セーブ復元
node test/ch0_tour.js         # 88項目：序章の通し＋仕掛け解き＋セーブ/ロード
node test/tune_umbra.js 200   # ウンブラ勝率の実測
```

**ぜんめつ判定の注意**：core は全滅しても町へ戻して全快させる。`aliveMembers()>0` では勝敗が分からない。メッセージ「ぜんめつ」で判定すること（tune_umbra.js がその実装）。

## 5. 実測ずみのバランス

**通しどおりの実測がいちばん大事**（Lv2で広場に入り、道を歩いてボスへ。ざこ戦で削られたまま戦う）

| 測りかた | 結果 |
|---|---|
| ボス前の到達Lv 分布（200回） | Lv3=27% / **Lv4=60%** / Lv5=13% |
| 道中の全滅 | 0/200 |
| **ウンブラ 勝率（通し）** | **91〜93%** |

全快・Lv固定での勝率：Lv3=69% ／ Lv4=99% ／ Lv5=100%。
§5の基準（到達Lv≈90% ／ 1つ下 50〜80%）を満たす。ウンブラは hp160 / atk11 / def8 / agi10（仕掛けを解く最短経路で測って調整）。
木人（trialdummy）は Lv1 のイオが確実に勝てる強さ（hp34 / atk3）。

**「いきなりボスでキツい」への対処（M0で実際に出た問題）**
- 広場を仕掛け付きダンジョンにした。解いて進むあいだに自然と4〜5戦する
- 序章ざこの経験値を約1.5倍にした（8/11/14/18/24）
- 逃げ遅れた男を入口に置き、「無理なら戻って宿で休め」と言わせる
- **満タン固定の勝率だけ見ていたのが誤り**。`tune_umbra.js` は通し実測を必ず出すこと

## 5-2. 「Ⅲよりクオリティが低い」への対処（M0で受けた指摘）

1. **裂け目の広場が水増しの廊下だった** → 分岐のある構造に作り直し。入口からボスまで51歩、行き止まり2本（宝箱／逃げ遅れた子ども）。**1マスの廊下に飾りの岩を置かない**（通れなくなる・脇道が抜け道になる不具合を2件出した）
2. **下層区が対称な箱だった** → 21×15に拡大。大通り・東西の路地・建物ブロック（宿／道具屋／教会／長屋）・光珠灯・広場・イオの家を配置
3. **表記がⅢのひらがなのままだった** → システム文をすべて漢字混じりに統一（攻撃／技／道具／防御／逃げる／経験値／回復…）。**イオ・セレンは「放った！」、ノエ・アマネは「唱えた！」**と職業で動詞を出し分け。`build_verify` に表記ゆれ検査を追加（Ⅲのひらがな13語が文字列に残っていないこと）

残っている課題：ボスの仮絵が中身と合っていない（木人＝ヌシガニ、ウンブラ＝Ⅲの影）。絵の用意時に解消する。

## 5-3. Ⅲからの進化：探索の仕掛け（採用した方針）

Ⅲは「歩く・戦う・話す」の3動作しかなく、ダンジョンが解く場所になっていなかった。Ⅳでは `specialTiles` 機構を土台に4種の仕掛けを実装した。

| タイル | 意味 | 挙動 |
|---|---|---|
| `O` | 動かせる岩 | 進行方向の先が床なら押せる。穴なら埋まって足場になる |
| `x` | 穴 | 通れない。岩を落とすと床になる |
| `L` / `l` | 光珠灯（消／点） | 調べると点灯・消灯。マップの灯りが全点灯で `lampGates` が発動 |
| `K` | 扉 | `locks` に鍵の指定があれば鍵で、なければ別の仕掛けで開く |

**タイル書き換えの仕組み**：`MAPS[map].tiles` の文字列を直接書き換える。描画・当たり判定・監査がすべてそのまま追従するので、読み替えの通し忘れが起きない。元の姿は `MAPS_ORIG` に保持し、`G.tileEdits` に差分を記録してセーブ/ロードで復元する。

**ソフトロック対策**：`gimmickRescue()` — 穴が1つも埋まっておらず、どの岩ももう押せない状態でマップに入ると、そのマップの仕掛けを元に戻す。詰んで進めなくなるのを防ぐ。穴のあるマップには岩を2つ以上置くことを監査で強制している。

**監査に追加した項目**：岩が最初から押せるか／穴のあるマップに岩が2つ以上あるか／まっすぐ押して届く穴があるか／光珠灯を調べられる位置か／扉に開ける手段が定義されているか。BFS到達性は `x` `O` `K` を「解けば通れる」ものとして扱う。

序章のダンジョンは両方の仕掛けを必須にした（岩で穴を埋めて中段へ → 光珠灯2つで奥の扉 → ボスの間）。

## 5-4. 行動範囲：天空大陸フィールド（`world`）

M0では町4枚しかなく、狭かった。**44×30・陸地650マスのフィールドマップ**を追加した。

- マップIDは `world`。LQ3のエンジンが `world` を特別扱いする箇所（地帯名表示・全滅時の帰還先）にそのまま乗る
- 周囲は雲海（`~`）で通行不能。**船はない**ので、陸続きでなければ到達不能になる。監査のBFS到達性を `world` にも適用している（LQ3では船前提で除外していた）
- 三段のたな（下層／中層／上層）を尾根と森で区切り、道で結ぶ。`encZones` で地帯名が変わる（下層の たな／中層の たな／上層の たな）
- 地点は6つ。**下層区（V）だけ入れる**。中層区・雲海港・光珠炉・天空城・空中庭園は `Q`（これから作る地点）で、調べると理由付きで断られる
- 下層区の東門（20,6）から出入りする。門番NPCが行き先を教える
- エンカウントは `encRate:0.09 / encGrace:4`（ダンジョンより緩め）。序章の悪夢獣が出る

M1で `Q` を順に実マップへ置き換えていく。

## 5-5. 見た目の進化：タイトル画面と戦闘演出

**タイトル画面（`ui.js` の `Title`）**
Ⅲは起動するといきなりメニューが1枚出るだけだった。Ⅳは専用のタイトル画面を挟む。雲海に浮かぶ三段の大陸シルエット、その上の城、明滅する光珠の灯り、空を横切る白竜、3層の雲海パララックス、明滅する星。**画像ファイルは使わずcanvasに全部描いている**ので、絵の支給を待たずに完成している。タップかキーでメニューへ進む。

**戦闘のダメージ表示（`view2d.js` の `pop2D` / `drawPops`）**
非ブロッキングの `V.pop({...})` を追加。戦闘中だけ数字が飛び出す。
- `dmg` 白／`crit` 金・1.5倍サイズ＋強いシェイクとフラッシュ／`pdmg` 味方の被弾は赤／`heal` 緑の `+n`／`status` 紫（「眠り！」など）／`miss` 灰
- 敵に当てた数字は敵スプライトの位置に、味方の被弾・回復は画面下のメンバー位置に出る
- 実測：ウンブラ戦40回で dmg 357 / heal 104 / crit 22 / pdmg 75 発火

Ⅲにもシェイクとフラッシュはあったが、**何がいくつ効いたかは文字を読まないと分からなかった**。数字が出ることで、buffやinflictの効果も画面で伝わる。

## 5-6. エンカウント率の基準（「遭遇が多すぎて進めない」への対処）

一度 encRate 0.24 / 猶予2歩まで上げてしまい、**6歩に1回**戦闘になって進めなくなった。レベルを稼がせようとして頻度で解決しようとしたのが誤り。**頻度は据え置き、1戦あたりの経験値で調整する**のが正しい。

| 場所 | encRate | 猶予 | 平均歩数/戦 |
|---|---|---|---|
| 裂け目の広場（ダンジョン） | 0.10 | 4歩 | 14歩 |
| 天空大陸（フィールド） | 0.07 | 5歩 | 19歩 |

序章ざこの経験値を2倍（16/22/28/36/48）、木人を40に引き上げて補償した。実測：広場を51歩歩くあいだの戦闘は平均4.0回、ボス前の到達Lvは3が中心、通し勝率90%。

## 5-7. 序章の後半（ウンブラ撃破後）

撃破直後は章末メッセージが出るだけだった。確定稿の「駆けつけたグランに認められて正式入団」を実際の手順として作った。

1. **撃破演出** — 紙片が地に着く前に消える描写。見物の夢守りの少年が顔色を変える（ノエの伏線）。広場の奥から騎士団の灯りが近づく
2. **グランが広場に現れる** — `bossReward.setTiles` で `rift_yard (12,3)` に `n` を追加。クエスト「団長への報告」が立つ
3. **逃げ遅れた子ども**（任意）— 礼に薬草2つと30ゴールド。父の鍛冶場の話が出て、イオの出自が生活のレベルで語られる
4. **町の反応が変わる** — 荷運び・技師・うわさずき・門番の台詞が撃破後／叙任後で三段階に変化。技師は「取り替えた新しい管も光が濁っている。この十年ずっとだ」と第1章の伏線を置く
5. **グランに報告** — 「怖かったか」「震えたまま立てたなら、それでいい」。HP/MP全快。「この裂け目は今夜だけのものではない」
6. **章末** — 翌朝の布告で叙任。セレンが「屋根の上で見ていた、動けなかった、次は下りる」と関係を進める。町は何もなかった顔で朝を迎え、夢守りの少年だけが灰の消えた場所を探している

**エンジンへの追加**：`setTiles` を `talkEvents` と `bossReward` の両方で使えるようにした（物語の進行でマスを書き換え、人を出したり道を開いたりできる）。`herbs` の受け取りにも対応。

**監査に追加**：npc.js の座標が実際に `n` タイルか、隣に立てるか。マップを作り直したとき座標が取り残されて「話しかけられないNPC」になる事故が実際に出たため（127項目）。

## 5-8. 一章の長さの基準（序章 v2）

序章が短すぎた（家→町→試験→ボス）。**節を7つ**に増やし、以降の章もこの分量を基準にする。

1. **家** — 隣のおばさん、宝箱
2. **炉技師の頼み**（新）— 東門の外で悪夢獣を3体。討伐ノルマ機構 `startQuota` を使用。報酬120G＋薬草2。伏線「あの影ども、管の近くにしか湧かん。光を嫌うなら逆だろうに、寄ってくるんだ」
3. **見習い試験・前半** — 木人。「いい太刀だ。腰が逃げていない」
4. **見習い試験・後半**（新）— **セレンとの模擬戦**。勝つと首席発表。「わたしは打ちにいった。それが差よ」「家格も見る。そういう決まりだ」
5. **上層の使者**（新）— うわさずきの男から。セレンの実家が首席発表を確かめに来ていた。「止めなかったわたしも同じ」
6. **裂け目の広場** — 仕掛け2種＋**中ボス「かげのあぎと」**（新・宝箱前の行き止まり／任意）→ ウンブラ
7. **撃破後** — 子ども、町の反応、グランへの報告、章末

**エンジンへの追加**：`bosses` のキーを `'マップ名:x,y'` にすると**1マップに複数のボスマス**を置ける（試験場に木人と模擬戦、広場に中ボスとウンブラ）。

**バランス**：ウンブラ通し勝率 89〜94%、道中の全滅0。おつかいでLvが上がる分、ボス前はLv3〜4が中心。

## 5-9. 序章 v3：かげのあぎとを軸にした二段構え

「かげのあぎと」を**二度出す**ことで、序章に縦の筋を通した。

**一度目（昼・光珠管の点検路）** — 新マップ。技師の依頼で東門の外へ。管に貼りついたあぎとが**光を吸っている**。イオが踏み込むと逃げられ、追えない。黒い紙片が一枚残る。**戦闘にはならない**（勝てない相手ではなく、届かない相手として置く）。

**技師への報告** — 「口だけ？　そいつは管を咥えていたか」「二十年この仕事をしてるが聞いたことがない」「……いや。一度だけある。お前の父さんが同じことを言っていた」「十年前だ。管の光が濁りはじめた年さ」。**父が同じものを見ていた**という一点で、イオの出自が物語に接続する。

**二度目（夜・裂け目の広場）** — 中ボスとして再登場。裂け目の光を吸って昼よりふくれている。「……お前か」「知ってるの」「昼に管に貼りついていた。逃げられた」。倒すとセレンが「光を食べる獣の真似、ね」「この街の光は、誰かに食べられてるのかも」と第2章の減光現象へつなぐ。

**新マップ `pipe_path`（光珠管の点検路）17×14** — 二重の回廊構造。世界地図の `X`（14,24）から入る。光珠灯2つを点けると奥の間が開き、あぎとに会える。討伐ノルマ3体もここで消化する。

これで序章の節は**8つ**：家 → 技師の依頼 → 点検路とあぎと初戦 → 報告 → 試験前半 → 模擬戦 → 上層の使者 → 裂け目（中ボス＋ウンブラ）→ 撃破後。

## 6. LQ3 から直したこと（IVでは再発させない）

1. **ボス報酬が「第2章以降だけ」動く決め打ち** → 全章で章データ駆動に統一
2. **NPC会話が「第1章だけ別扱い」** → 撤去。`questOnTalk` は `runTalkEvent` を呼ぶだけ
3. **defdown が MP を二重に引く** → 修正（LQ3本体は未修正）
4. world.js の**重複キー `elde`** → 除去（監査が検出）
5. LQ3固有のハードコードを削除：こぶね・ゆめみ・王女・玉座・第1章の締めくくり・デスグラン3連戦・交易相場・M0技術スライス残骸

## 7. 未着手・要判断

1. **絵**：`assets.js` は lq3 の流用。イオ／セレン／ノエ／アマネ、悪夢獣、ウンブラの新規絵が必要（カタログPNG承認後に組込）
2. **BGM**：lq3 の6曲（field/town/castle/dungeon/battle3/boss）を**暫定流用**。新規作曲後に同じ名前で差し替えれば鳴る
3. **タイル**：天空様式（白石・光珠灯・雲海縁）は未作成。いまは lq3 の village/dream テーマを流用
4. ~~とこよの ゆめ~~ → **決着**。敵の状態は1枠しか持てないため、`inflict` に `st2`（1つめが外れた／すでに掛かっている時の2つめ）を追加して表現する。`tokoyo` は st:'sleep' p0.35 ／ st2:'slow' p2:0.55。**必ず何かは入る上位技**として成立させた
5. **オート戦闘（gungan/inochi）が inflict・buff を使わない**：`autoCommand` に判断を足す。→ **M1 で入れる**（決定ずみ）
6. 飛翔グライダー・騎士団任務ランク・悪夢侵食度・行商ミニは M1 以降

## 8. デプロイ手順（まだ実行していない）

1. **最新コミットを確認**。想定外のコミットがあれば中断して確認（並行セッション対策）
2. GitHub Contents API で PUT（GET sha → PUT）
3. Pages ビルドを `status=='built'` までポーリング
4. 拾われないときは `lq4/BUILD.txt` を更新して再ビルドを起こす
5. 納品連絡に毎回「アプリ再起動で反映・セーブ維持」を添える

## 9. 地雷集

構想書 §10 を正とする。本書では重複管理しない。
